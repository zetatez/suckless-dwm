#!/bin/sh

mkdir -p ~/repos/github/
cd ~/repos/github

repos=$(fd --type d --hidden -d 1)

for repo in $repos; do
  {
    echo "cd $repo && git pull; cd .."
    cd $repo
    git pull
    git-pull-all
    cd ..
  } &
done

wait
